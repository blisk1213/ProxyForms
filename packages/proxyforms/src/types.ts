import { Author, Category, Post, PostWithContent, Tag } from "@proxyforms/types";

// These are exported here so users don't have to install @proxyforms/types

export type { Author, Category, Post, PostWithContent, Tag };
